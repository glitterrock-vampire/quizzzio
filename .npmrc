# Force npm to use legacy peer deps (helps with React 18 and Vite)
legacy-peer-deps=true

# Ensure package-lock.json is generated with exact versions
package-lock=true

# Use the latest npm for installation
engine-strict=true
